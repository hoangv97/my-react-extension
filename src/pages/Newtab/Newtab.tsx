import Window from '@/components/common/window';
import React from 'react';

const Newtab = () => {
  return (
    <div className="min-w-full min-h-screen">
      <Window x={0} y={0} width={230} height={123}>
        1231231
      </Window>
    </div>
  );
};

export default Newtab;
